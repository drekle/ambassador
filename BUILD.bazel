package(default_visibility = ["//visibility:public"])

# gazelle:proto package

load("@bazel_gazelle//:def.bzl", "gazelle")

gazelle(
    name = "gazelle",
    external = "vendored",
    prefix = "github.com/datawire/ambassador",
    extra_args = [
        "-lang",
        "proto",
    ],
)
